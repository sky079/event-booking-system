services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ebs
      POSTGRES_USER: ebs
      POSTGRES_PASSWORD: ebs
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: